# Cell demo, full world: the slice-1 evidence doc's companion

Slice-1's binding statement was: **the world was REDUCED (19 declared
excludes, ~110 files); full-world acceptance is gated on CX-tj6b.** This
is the companion that closes it: the same ceremony at FULL WORLD, run
2026-08-11 ~19:55–20:13Z, everything below measured from the store.

## The scope decision (plan's category split, msg 11253)

The 19 excludes were never one population:

- **RETIRED (17)** — memory (CX-tj6b), binary-crash (CX-g8r1), and
  substrate-mint (CX-fadm) WORKAROUNDS, each defect fixed and landed
  before this run: `.github .stateproj bd chat bin data demo docs
  scripts tmp tools fixtures commonplace commonplace_bots
  commonplace_cli commonplace_mcp commonplace_web`.
- **KEPT (2)** — the secrets-class POLICY declarations, which never
  retire: `.beads` (123 credential-adjacent `.darc` archives — the
  CX-fm7x finding itself) and `.claude` (operator-private session
  state). Declared as `PROTO_CHIT_SYNC_EXCLUDES=".beads,.claude"` on
  top of the emitter's un-droppable defaults
  (`.git,.commonplace,_build,deps`).

## Cell-3: born `:minimal` — the class doing the work excludes used to do

Cell-3 (root `0eadae3e`, principal `0f38d1b7`, event-log `2c3a1ab2`)
was born via `commonplace init --profile minimal` — S2v4's flag, first
real use. What the class refused, live, with the S2v4 refusal text:
`bd`, `chat` (CX-fadm's mints — the workaround excludes are obsolete
BY DESIGN), and initially `__reflog` (below).

## The full-world emission

- **1,231 files → 22 root entries** under `:minimal` negation-allow;
  content read back through nested schemas.
- **43s wall (first run) / 33s (re-run), under a 6GB systemd ceiling.**
  The pre-tj6b measurement on this same world: >10GB RSS, never
  converged, twice, and it killed the operating session. CX-tj6b's fix
  (@c84983d) is what made this paragraph possible.
- **CX-g8r1's population handled by the S1a floor**: every non-UTF-8
  file produced a named per-file skip (`excluded-binary`) —
  `.bin` fixtures, `.cub` stores, `favicon.ico`, escripts. Honest,
  incomplete, declared — the S23 artifact-reference work replaces the
  floor's population; the floor never silently disappears.

## The composition finding this run earned (the demo's whole point)

The first emission's event leg FAILED: `cut_pin` rides
`Reflog.Snapshot.checkpoint`, which attaches `__reflog` to the root —
and `:minimal` refused it, correctly, per the registered set. The event
WALed as an unsigned intent (CX-z0sa's fix refusing a false success).
Plan ruled the collision (msg 11310): **the pin is not furniture the
cell declined — it is the cell's core product**, so `__reflog` joined
the `:minimal`-accepted set by declared amendment (@e2a6e0e); the
registered set had been seeded from a DEFAULT workspace's inventory and
cells were not in the measured population — under-scoped, not wrong.
The off-root pin is filed as a chit-arc design question
(reachability-is-liveness). Riders ratified for follow-up rounds: the
refusal reached the operator as a bare `emission failed: :error`
(loudness defect in the cut_pin/Snapshot/Restore chain), and the Bd
invariants engine FunctionClauseErrors per-validation on a
class-refused world (needs class-aware skip).

## The event, verified from the store (never from the tap line)

Re-run after the amendment: `tap fired c19dd555…`, and read back from
event-log `2c3a1ab2` via the RedLog doc:

- Signed event, `author-principal` = cell-3's principal `0f38d1b7`.
- **The proto-pin enumerates the imported world per-file as
  `{doc, commit}`** (checkpoint doc `e459ad25`, commit `054a9119…`) —
  the CX-fm7x invariant at full scale: imported = enumerable from the
  pin; excluded = the 2 declared names + 4 defaults, never inferred.
- Predecessor-ref present (first event on the branch: empty ref list).
- The first attempt's WAL envelope (1 pending, unsigned intent) remains
  in `state-full/` as evidence, loudly announced by F1 on every shim
  invocation — its disposition (replay vs archive) is an operator
  follow-up, not silent.

## Enclosure

Same box, same operator, serve pid 3671069 (bba7d56) uninvolved in the
emission path; ceremony under `systemd-run --user --scope -p
MemoryMax=6G`; emitter built from main @e2a6e0e (first run @6d92e11).
Raw logs: `/home/jes/cell-1/evidence-full/` (birth, keygen, both
commits, RSS sampler, retry trace). Cell-2's store preserved at
`/home/jes/cell-1/store-attempt2`.

## What this does NOT claim

Slice 2 (second workspace over federation) is the next rung, gated on
this rung — now green. The witness/enclosure rules carry over. The pin
lives at `__reflog`-on-root today by ruled amendment; the off-root
design question is open in the chit arc. "Demo green at full world"
still does not claim multi-node cells work — that is slice 2's claim
to earn.
