# BUILD BRIEF — CX-1n8y: the sweep summary counts ATTEMPTS and prints them as VERDICTS

**For:** Sol (codex)
**Ticket:** **CX-1n8y** (p1/bug)
**File:** `apps/commonplace/lib/commonplace/projection/mixed_plane_history.ex`

---

## 0. Environment contract (standing)

Named worktree off **current** `origin/main`; ⛔ git metadata read-only, **leave
changes UNSTAGED**, no `git add`, no commit, **do not work around
`index.lock`**; ⛔ no route to the live serve or live store — **build your own
fixture stores**; `bin/cp-test-guard --min N --apps N -- <cmd>` for every suite,
**one at a time** (shared `tmp/test_data`); ⚠️ **rc from the command itself,
never a pipe**; ⚠️ **a count from a piped listing is not a count** (`wc -l`
unpiped; `head` only to look).

## 1. ⛔ THE INVARIANT — restate it in your report

> **The summary must be STRUCTURALLY INCAPABLE of reporting full coverage when
> coverage was partial.**

Not "should mention skips." **Incapable.** The current summary is a true
sentence describing something other than what a reader takes it for, and the
fix is to make the misreading impossible rather than to add a line someone may
or may not read.

## 2. The defect, at the line

`build_record/…` (~:265-284) folds over a doc's commits:

```elixir
{:ok, _bytes, _verdict} ->
  {hits, failures, attempted + 1}

other ->
  failure = %{"commit_id" => hex(commit_id), "reason" => inspect(other)}
  {hits, [failure | failures], attempted + 1}      # ← ALSO increments
```

⇒ **`attempted` increments on BOTH branches**, and is then stored as
`"commits_scanned"`. **So `commits_scanned` counts commits the sweep TRIED, not
commits that produced a verdict.**

⭐ **The tell, and it is exact:** because every commit is attempted,
`commits_scanned` **always equals** `commits_total` (`summarize/4`, ~:356-358:
`commits_total` is the length of all `commit_ids`). **Two numbers printed side
by side in the same summary line that can never disagree.** On the first live
run both read **25,995** while **4,719 commits (18.15%) produced no verdict**.

**What the summary omits entirely:** `skip_reasons` is a per-doc list and
**nothing totals it.** ⇒ Real coverage was **81.85%** and the line reads as a
complete answer over complete history.

⚠️ **And a percentage would still under-report the shape:** **78 documents were
FULLY unscanned** — every commit skipped — while 81 were partially skipped. **A
per-commit percentage lets a reader imagine partial visibility everywhere; the
truth was zero visibility on 78 docs.**

⚠️ Note `docs_scanned = total - length(skipped)` where `skipped` is *any doc
with ≥1 skip* — so that field is conservative in the right direction, but it
**conflates fully-unscanned with partially-skipped**, which are the two facts a
reader most needs separated.

**Not a scanner bug:** every skip was `{:unknown, {:conflicted, …}}` — a
projection/trust verdict. Those commits declined to yield state, and
`MixedPlane.scan/1` runs on reconstructed state. ⇒ **It is a coverage limit the
scanner does not surface.** Do not try to make conflicted commits project.

## 3. Work

1. **`commits_scanned` must count VERDICTS.** Increment only on the `{:ok, …}`
   branch, or rename the existing field to `commits_attempted` and add a real
   `commits_scanned`. **Say which you chose and why** — one of them changes a
   persisted checkpoint field's meaning, which matters for resumed sweeps.
2. **Total the skips** and surface them: a `commits_skipped` and an explicit
   **coverage** figure.
3. ⭐ **Separate the two doc populations** the current `docs_skipped` merges:
   **fully unscanned** (every commit skipped — the sweep learned *nothing*
   about these) vs **partially skipped**.
4. ⚠️ **Check `PROGRESS` (~:343) too** — it sums the same field and inherits
   the same defect.

## 4. Acceptance — red-first, paste real output

1. ⭐ **A test with a fixture store where SOME commits fail to project**, asserting
   the summary reports the skips and a coverage figure below 100%.
   ⛔ **Demonstrate it RED against the current implementation**, where it will
   report full coverage on a store you know is partially unscanned. **That red
   is the ticket.**
2. ⭐ **A test that `commits_scanned` and `commits_total` CAN DIVERGE.** Today
   they are arithmetically incapable of it — that equality is the defect's
   signature, so pin the property that makes it possible.
3. ⭐ **A test distinguishing fully-unscanned from partially-skipped docs** —
   a fixture with at least one of each, asserting they are reported separately.
4. Named suites green, with counts, one at a time:
   - `apps/commonplace/test/commonplace/projection` — **on-main count: report
     what you measure before changing anything**
   - `apps/commonplace/test/commonplace/projection/mixed_plane_history_test.exs`
5. `mix compile --warnings-as-errors` rc=0.
6. ⚠️ **If changing the checkpoint field's meaning would misread EXISTING
   persisted checkpoints, say so and stop** — a resumed sweep reading an old
   checkpoint under new semantics is a silent wrong answer, which is the same
   class as the bug being fixed.
7. **Name anything you could not verify in-sandbox** and stop rather than
   approximating. The live sweep is not reproducible here; a fixture store is
   representative because this defect is arithmetic over records.

## 5. Out of scope

- Making conflicted commits project — that is a trust/projection question.
- The oracle-vs-incremental strategy selection.
- Any other defect: **report it, don't fix it.**

⚠️ **One irony worth carrying, from the ticket:** this tool was **deliberately
built to report its own denominators** so it could not inherit a stale one. It
does report a denominator — **and it is the wrong one.** *Build the denominator
from what ARRIVED, not from what SURVIVED* was the rule, and this is the rule's
own instrument failing it.
