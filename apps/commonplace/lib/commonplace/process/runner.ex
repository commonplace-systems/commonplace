defmodule Commonplace.Process.Runner do
  @moduledoc """
  Execute processes in one of three modes:

  - :evaluate — run code with restricted access
  - :sandbox  — run command inside a sync sandbox
  - :command  — raw execution (no sandbox)
  """
end
