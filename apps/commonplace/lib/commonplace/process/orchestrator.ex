defmodule Commonplace.Process.Orchestrator do
  @moduledoc """
  Reads __processes.json documents and manages process lifecycles.

  The orchestrator watches for changes to process configuration
  documents and starts/stops process runners accordingly.
  """
end
